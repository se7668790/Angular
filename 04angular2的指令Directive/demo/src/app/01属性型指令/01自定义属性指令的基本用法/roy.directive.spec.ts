import { RoyDirective } from './roy.directive';

describe('RoyDirective', () => {
  it('should create an instance', () => {
    const directive = new RoyDirective();
    expect(directive).toBeTruthy();
  });
});
